# CherryPick Workshop — комплект Telegram-бота учёта времени

Публичный канал выдачи комплекта (зеркало каталога `bot/` закрытого монорепо) и релизов валидатора.
Заказчик копирует ОДИН файл — клиентскую копию `workshop-bot-bootstrap.yml` из ассетов релиза — в
`.github/workflows/` своего репозитория, кладёт два секрета и карту людей; остальное делает workflow.
Инструкция — `bot/kit/docs/bot-user-guide.ru.md`. Пины релиза — `bot/kit/validator-release.yaml`.
